
# Agent session data — disposable by construction; anything durable is promoted into .claude/context/
.agents/

# Machine-specific tooling config, generated by scripts/setup-tooling.sh. Each
# resolves to a DerivedData path hashed from this checkout's location, so a
# committed copy points at another developer's disk. Regenerate, never share.
.xcodebuildmcp/config.yaml
buildServer.json
.compile
