---
name: no-heading
description: Use when proving that the validator rejects a body with no H1 and a force-loading @-link.
---

This fixture has no H1, so every heading below reads as top level.

It also force-loads @references/detail.md instead of linking it by plain path.
